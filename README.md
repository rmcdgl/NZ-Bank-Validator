# Payments NZ register history

This branch stores derived monthly snapshots of active Payments NZ bank/branch pairs.

It is generated by the scheduled register coverage workflow and should not be merged into `main`.
Snapshot files intentionally contain only normalized active bank/branch IDs, not the full Payments NZ CSV.
